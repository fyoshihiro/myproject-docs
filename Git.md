# Git

Gitのインストール(Windows)から基本的な使い方、`.gitignore`、コマンド一覧、初心者がつまずきやすい用語の解説までをまとめます。

## 目次

- [インストール(Windows)](#インストールwindows)
- [基本的な使い方](#基本的な使い方)
- [.gitignore](#gitignore)
- [コマンド一覧](#コマンド一覧)
- [用語集](#用語集)

---

## インストール(Windows)

### 1. Gitをダウンロード・配置する

「64-bit Git for Windows Portable」をダウンロードし、任意の場所に解凍します(例: `C:\Git`)。

### 2. 環境変数PATHに追加する

コマンドプロンプトやPowerShellからどこでも `git` コマンドを実行できるよう、環境変数PATHに `C:\Git\bin` を追加します。

### 3. 動作確認する

```bash
git --version
```

バージョン番号が表示されれば、インストールとPATH設定が正しくできています。

### 4. VSCodeにGitのパスを設定する

1. VSCodeで `F1` キーを押してコマンドパレットを開く
2. 「setting」と入力して設定画面を開く
3. 検索欄で「git path」を検索し、`C:\Git\bin\git.exe` を追記

### 5. ユーザー名・メールアドレスを登録する

コミット時の記録者情報として使われる名前とメールアドレスを設定します。

```bash
git config --global user.name "任意のユーザー名"
git config --global user.email "任意のメールアドレス"
```

設定内容を確認します。

```bash
git config --list
```

---

## 基本的な使い方

### ローカルリポジトリを作成してGitHubに公開する

```bash
git init .
git add .
git commit -m 'first commit'
git remote add origin https://github.com/xx/xx.git
git push -u origin main
```

| コマンド | 内容 |
|---|---|
| `git init .` | カレントディレクトリをGitリポジトリとして初期化する |
| `git add .` | カレントディレクトリ以下の変更をすべてインデックス(コミット対象)に追加する |
| `git commit -m 'first commit'` | インデックスに追加した内容を、メッセージ付きでリポジトリに記録する |
| `git remote add origin <URL>` | リモートリポジトリ(GitHubなど)を `origin` という名前で登録する |
| `git push -u origin main` | ローカルの `main` ブランチをリモートの `origin` にプッシュし、以降 `git push` だけで済むよう追跡設定(`-u`)する |

### 既存リポジトリをクローンする

```bash
git clone https://github.com/xx/xx.git
```

### ブランチを作成・切り替える

```bash
git branch feature1     # ブランチを作成
git checkout feature1   # 作成したブランチに移動
git branch              # 現在のブランチを確認(先頭に * が付いているものが現在地)
```

---

## .gitignore

Gitの管理対象から除外したいファイル・ディレクトリ(ビルド成果物、環境依存の設定ファイルなど)を指定するための設定ファイルです。

### 1. `.gitignore` を編集する

除外したいファイル・ディレクトリのパターンを記述します。

### 2. すでに追跡されているファイルのキャッシュを削除する

`.gitignore` は**まだGitに登録されていないファイル**にのみ効果があります。すでに `git add` 済み(追跡中)のファイルを除外対象にしたい場合は、一度インデックスから削除する必要があります。

```bash
git rm -r --cached .
```

このコマンドは実ファイルを削除するのではなく、あくまで**Gitの追跡対象(インデックス)から外す**操作です。この後 `.gitignore` の内容に従って再度 `git add` すれば、除外したいファイルだけが対象から外れた状態で追跡されます。

### 3. コミット・プッシュする

```bash
git add .
git commit -m 'apply .gitignore'
git push
```

---

## コマンド一覧

### 使用法(オプション)

| コマンド | 内容 |
|---|---|
| `git -v` | バージョンを表示 |
| `git -h` | ヘルプを表示 |
| `git -C <パス>` | リポジトリの場所を指定して実行 |
| `git --html-path` | HTMLドキュメントの場所を表示 |
| `git --man-path` | マニュアルの場所を表示 |
| `git --info-path` | 情報ファイルの場所を表示 |
| `git -p` | `--paginate` の短縮形。出力をページャーで表示 |
| `git -P` | `--no-pager` の短縮形。ページャーを使わず出力 |
| `git --bare` | 作業ツリーを持たないリポジトリとして扱う |
| `git --git-dir=<パス>` | `.git` ディレクトリの場所を指定 |
| `git --work-tree=<パス>` | 作業ツリーの場所を指定 |

### 作業領域

| コマンド | 内容 |
|---|---|
| `git clone <URL>` | リポジトリのクローンを新しいディレクトリに作成する |
| `git init [directory]` | 空のGitリポジトリを作成、または既存リポジトリを再初期化する |

### 変更操作

| コマンド | 内容 |
|---|---|
| `git add` | ファイルの内容をインデックスに追加する |
| `git mv` | ファイル・ディレクトリ・シンボリックリンクの移動または名前変更 |
| `git restore` | 作業ツリーのファイルを復元する |
| `git rm` | 作業ツリーとインデックスからファイルを削除する |

### 履歴と状態

| コマンド | 内容 |
|---|---|
| `git bisect` | 二分探索でバグを引き起こしたコミットを特定する |
| `git diff` | コミット間、コミットと作業ツリーの間などの差分を表示する |
| `git grep` | パターンに一致する行を出力する |
| `git log` | コミットログを表示する |
| `git show` | コミットなど、さまざまな種類のオブジェクトの内容を表示する |
| `git status` | 作業ツリーの状態(変更・未追跡ファイルなど)を表示する |

### 変更管理

| コマンド | 内容 |
|---|---|
| `git branch` | ブランチの一覧表示・作成・削除 |
| `git commit` | 変更をリポジトリに記録する |
| `git merge` | 2つ以上の開発履歴(ブランチ)を統合する |
| `git rebase` | 別のコミットの上にコミットを再適用する |
| `git reset` | 現在のHEADを指定した状態にリセットする |
| `git switch` | ブランチを切り替える |
| `git tag` | タグの作成・一覧表示・削除・検証 |

### コラボレーション

| コマンド | 内容 |
|---|---|
| `git fetch` | 別のリポジトリからオブジェクトと参照(履歴情報)をダウンロードする |
| `git pull` | 別のリポジトリまたはローカルブランチから取得し、統合する |
| `git push` | リモート参照と関連オブジェクトを更新する |

---

## 用語集

Gitを学び始めたときにつまずきやすい用語をまとめます。

### リポジトリ

ファイルや変更履歴を保存しておくデータベースのこと。手元の環境にある**ローカルリポジトリ**と、ネットワーク上にある**リモートリポジトリ**がある。自分のPC上でソースコードを編集する際はローカルリポジトリを使い、他のユーザーとファイル・変更履歴を共有する際はリモートリポジトリを使う。

### コミット

ファイルや変更履歴をリポジトリへ登録すること。

### プッシュ

コミットをローカルリポジトリからリモートリポジトリへ反映させること。プッシュにより、ローカルの変更履歴がリモートリポジトリへアップロードされる。

### プル

リモートリポジトリのコミットをローカルリポジトリへ反映させること。プルにより、リモートの変更履歴がローカルリポジトリへダウンロードされる。

### クローン

リモートリポジトリの内容を、ローカルリポジトリへまるごとコピーすること。プルとの違いは、プルが差分のみをコピーするのに対し、クローンはリポジトリ全体をコピーする点にある。そのため、まだ何も登録されていないローカル環境にコピーしたいときはクローンを使う。

### ブランチ

同一リポジトリ内で変更履歴を分岐させること、またはその分岐そのもの。ブランチを作成して変更を加えても、メインのブランチには影響しない。バグ修正や機能追加など、ソースコードのバージョン管理を分けて進めたいときに使う。

### マージ

作成したブランチをメインのブランチへ統合すること。これにより、ブランチ側で行った変更内容がメインのブランチに反映される。